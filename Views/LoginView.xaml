<Window x:Class="WarehouseManagementUnia.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login" Height="300" Width="400">
    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Text="Username:" Margin="10"/>
            <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Width="200" Margin="10"/>
            <TextBlock Text="Password:" Margin="10"/>
            <PasswordBox x:Name="PasswordBox" Width="200" Margin="10" KeyDown="PasswordBox_KeyDown"/>
            <Button Content="Login" Command="{Binding LoginCommand}" CommandParameter="{Binding ElementName=PasswordBox}" Width="100" Margin="10"/>
        </StackPanel>
    </Grid>
</Window>