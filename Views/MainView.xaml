<Window x:Class="WarehouseManagementUnia.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Warehouse Management - Unia" Height="600" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Navigation Bar -->
        <StackPanel Orientation="Horizontal" Background="LightGray">
            <Button Content="Stock" Command="{Binding NavigateCommand}" CommandParameter="Stock" Margin="5"/>
            <Button Content="Contractors" Command="{Binding NavigateCommand}" CommandParameter="Contractors" Margin="5"/>
            <Button Content="Documents" Command="{Binding NavigateCommand}" CommandParameter="Documents" Margin="5"/>
            <Button Content="Empty" Command="{Binding NavigateCommand}" CommandParameter="Empty" Margin="5"/>
            <TextBlock Text="{Binding CurrentViewTitle}" VerticalAlignment="Center" Margin="10"/>
        </StackPanel>

        <!-- Content Area -->
        <ContentControl Grid.Row="1" Content="{Binding CurrentView}"/>
    </Grid>
</Window>