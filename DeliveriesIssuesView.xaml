<Window x:Class="WarehouseManagementUnia.DeliveriesIssuesView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Dostawy i wydania" Height="700" Width="800" WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <GroupBox Grid.Row="0" Header="Dodaj dostawę" Margin="0,0,0,10">
            <StackPanel Margin="5">
                <Label Content="Produkt"/>
                <ComboBox x:Name="DeliveryProductComboBox" Width="300" DisplayMemberPath="Name" Margin="0,0,0,5"/>
                <Label Content="Kontrahent"/>
                <ComboBox x:Name="DeliveryContractorComboBox" Width="300" DisplayMemberPath="Name" Margin="0,0,0,5"/>
                <Label Content="Ilość"/>
                <TextBox x:Name="DeliveryQuantityTextBox" Width="300" Margin="0,0,0,5"/>
                <Label Content="Data dostawy"/>
                <DatePicker x:Name="DeliveryDatePicker" Width="300" Margin="0,0,0,5"/>
                <Label Content="Opis"/>
                <TextBox x:Name="DeliveryDescriptionTextBox" Width="300" Margin="0,0,0,10"/>
                <Button Content="Dodaj dostawę" Width="100" Click="AddDelivery_Click"/>
            </StackPanel>
        </GroupBox>
        <GroupBox Grid.Row="1" Header="Dodaj wydanie" Margin="0,0,0,10">
            <StackPanel Margin="5">
                <Label Content="Produkt"/>
                <ComboBox x:Name="IssueProductComboBox" Width="300" DisplayMemberPath="Name" Margin="0,0,0,5"/>
                <Label Content="Kontrahent"/>
                <ComboBox x:Name="IssueContractorComboBox" Width="300" DisplayMemberPath="Name" Margin="0,0,0,5"/>
                <Label Content="Ilość"/>
                <TextBox x:Name="IssueQuantityTextBox" Width="300" Margin="0,0,0,5"/>
                <Label Content="Data wydania"/>
                <DatePicker x:Name="IssueDatePicker" Width="300" Margin="0,0,0,5"/>
                <Label Content="Opis"/>
                <TextBox x:Name="IssueDescriptionTextBox" Width="300" Margin="0,0,0,10"/>
                <Button Content="Dodaj wydanie" Width="100" Click="AddIssue_Click"/>
            </StackPanel>
        </GroupBox>
        <GroupBox Grid.Row="2" Header="Transakcje">
            <DataGrid x:Name="TransactionsDataGrid" AutoGenerateColumns="False" IsReadOnly="True" Margin="5">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Typ" Binding="{Binding Type}" Width="80"/>
                    <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                    <DataGridTextColumn Header="Produkt" Binding="{Binding ProductName}" Width="120"/>
                    <DataGridTextColumn Header="NIP kontrahenta" Binding="{Binding ContractorNIP}" Width="100"/>
                    <DataGridTextColumn Header="Ilość" Binding="{Binding Quantity}" Width="60"/>
                    <DataGridTextColumn Header="Data" Binding="{Binding Date, StringFormat=yyyy-MM-dd}" Width="80"/>
                    <DataGridTextColumn Header="Opis" Binding="{Binding Description}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>
    </Grid>
</Window>