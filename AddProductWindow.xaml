<Window x:Class="WarehouseManagementUnia.Views.AddProductWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Dodaj Produkt" Height="300" Width="400">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Nazwa:" Grid.Row="0"/>
        <TextBox x:Name="NameBox" Grid.Row="0" Margin="60,0,0,0"/>

        <TextBlock Text="Kategoria:" Grid.Row="1"/>
        <TextBox x:Name="CategoryBox" Grid.Row="1" Margin="60,0,0,0"/>

        <TextBlock Text="Ilość:" Grid.Row="2"/>
        <TextBox x:Name="QuantityBox" Grid.Row="2" Margin="60,0,0,0"/>

        <Button Content="Dodaj" Grid.Row="3" HorizontalAlignment="Right" Width="80" Click="Add_Click"/>
    </Grid>
</Window>
